define([], function () {

	describe('module: ~sampleModule', function () {

		beforeEach(function () {
			// Create test set-up
		});

		afterEach(function () {
			// Do some cleanup
		});


		it('should always pass', function () {
			expect(1).toEqual(1);
		});
	});
});
